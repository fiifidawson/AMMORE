[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ammore"
version = "0.1.0"
description = "Agentic literature review on top of mmore."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "autogen-agentchat>=0.7.0",
    "autogen-ext[openai,ollama]",
    "dspy>=2.5",
    "mmore[process,index,rag,api]",
    "milvus-lite<3; platform_system != 'Windows'",
    "pymilvus",
    "python-dotenv",
    "pyyaml",
    "requests",
    "tavily-python",
]

[project.optional-dependencies]
dev = [
    "pre-commit",
    "pyright",
    "pytest",
    "ruff",
]

[project.scripts]
ammore = "ammore.main:main"

[tool.uv]
# pillow<11 needed by mmore[process]/marker-pdf; autogen-core asks for >=11 but works with 10
override-dependencies = ["pillow>=10.1.0,<11.0.0"]
torch-backend = "cpu"

[tool.setuptools.packages.find]
include = ["ammore*"]

[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N"]
fixable = ["ALL"]
ignore = ["E501", "E402"]   # line length, module-level imports not at top

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]

[tool.pyright]
include = ["ammore", "evaluate.py"]
exclude = ["**/__pycache__", ".venv"]
pythonVersion = "3.11"
typeCheckingMode = "basic"
reportMissingImports = "warning"
reportMissingTypeStubs = false
# AutoGen's BaseAgentEvent/BaseChatMessage don't declare .content but expose it at runtime.
reportAttributeAccessIssue = "none"
